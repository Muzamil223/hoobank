export type NotificationType = 'success' | 'info' | 'warning' | 'error';
export interface Notification {
    id: number;
    optionalTimeOverRide?: number;
    type: NotificationType;
    title: string;
    message: string;
}
