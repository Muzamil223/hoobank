/// <reference types="react" />
import { ISubmenuButton } from '../MenuBar.types';
export interface IMenuButtonProps {
    icon?: JSX.Element;
    label?: string;
    tooltipText: string;
    submenus?: ISubmenuButton[];
    renderSubmenuFromBottomUp?: boolean;
    onClick?: () => void;
    path: string | null;
    changeRoute?: (path: string) => void;
}
export default function MenuButton({ onClick, icon, label, tooltipText, submenus, renderSubmenuFromBottomUp, changeRoute, path, }: IMenuButtonProps): JSX.Element;
