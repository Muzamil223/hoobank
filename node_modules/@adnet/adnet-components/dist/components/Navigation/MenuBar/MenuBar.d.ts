/// <reference types="react" />
import { IMenuButton } from './MenuBar.types';
interface IMenuBar {
    menuButtons: IMenuButton[];
    isLoggedIn: boolean;
    setIsLoginOpen: React.Dispatch<React.SetStateAction<boolean>>;
    isLoginOpen: boolean;
    onLoginClick: (username: string, password: string) => void;
    changeRoute?: (path: string) => void;
    hasLoginErrorOccured: boolean;
}
export default function MenuBar({ menuButtons, isLoggedIn, setIsLoginOpen, isLoginOpen, onLoginClick, changeRoute, hasLoginErrorOccured, }: IMenuBar): JSX.Element;
export {};
