/// <reference types="react" />
export interface IMenuButton {
    path: string | null;
    label?: string;
    icon?: JSX.Element;
    tooltipText: string;
    location: MenuLocation;
    submenus?: ISubmenuButton[];
    renderSubmenuFromBottomUp?: boolean;
    onClick?: () => void;
    changeRoute?: (path: string) => void;
}
export declare enum MenuLocation {
    UP = "upperArea",
    DOWN = "lowerArea"
}
export declare enum Path {
    home = "/home",
    login = "/login",
    auctionProcess = "/auctionProcess",
    activation = "/activation"
}
export interface ISubmenuButton {
    text: string;
    path: string;
    visible: boolean;
}
export declare const userPermissions: {
    ShowProductDefinition: string;
    ShowAuctions: string;
    ShowAuctionsBidding: string;
    ShowContracts: string;
    ShowAuctionTemplates: string;
    ShowAuctionList: string;
    ShowEventsList: string;
    ShowDocumentExport: string;
};
