import { IIconProps } from '../Icon.type';
interface IStopIcon extends IIconProps {
    title?: string;
}
declare const StopIcon: ({ onClick, isHidden, color, rotate, size, title, }: IStopIcon) => import("react/jsx-runtime").JSX.Element;
export default StopIcon;
