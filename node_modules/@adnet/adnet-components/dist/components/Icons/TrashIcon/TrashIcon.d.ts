import React from 'react';
import { IIconProps } from '../Icon.type';
interface ITrashIcon extends IIconProps {
    manual?: {
        width: string;
        height: string;
        preserveAspectRatio?: boolean;
    };
    style?: React.CSSProperties;
    title?: string;
}
export declare function TrashIcon({ onClick, isHidden, color, rotate, size, title, ...props }: ITrashIcon): import("react/jsx-runtime").JSX.Element;
export {};
