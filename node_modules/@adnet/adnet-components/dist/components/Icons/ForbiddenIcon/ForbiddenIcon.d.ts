import React from 'react';
import { IIconProps } from '../Icon.type';
interface ForbiddenIcon extends IIconProps {
    manual?: {
        width: string;
        height: string;
        preserveAspectRatio?: boolean;
    };
    style?: React.CSSProperties;
}
export declare function ForbiddenIcon({ isHidden, color, onClick, size, rotate, ...props }: ForbiddenIcon): import("react/jsx-runtime").JSX.Element;
export {};
