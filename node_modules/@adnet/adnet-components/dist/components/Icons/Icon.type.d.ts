/// <reference types="react" />
import { ComponentState } from '../../constants/ComponentState';
import { Rotate } from '../../constants/Rotate';
import { Size } from '../../constants/Size';
export interface IIconProps {
    /** A callback function of type React.MouseEventHandler to be executed when the drop-down icon is clicked. */
    onClick?: React.MouseEventHandler;
    /** A boolean value that, when true, hides the drop-down icon. The default value is false. */
    isHidden?: boolean;
    /** A string representing the color of the drop-down icon. It should be a valid CSS color value. The default color is undefined. */
    color?: string;
    /** A boolean value that, when true, rotates the drop-down icon. The default value is false. */
    rotated?: boolean;
    /** Rotate enum for choosing Icon direction */
    rotate?: Rotate;
    /** Optional size parameter */
    size?: Size;
    /** Component state that could cascade into the icon */
    componentState?: ComponentState;
    /** Classname provider */
    className?: string;
    /** Background color if possible */
    backgroundColor?: string;
}
export interface ILogoProps extends IIconProps {
    showCompanyName: boolean;
    useDefaultColors: boolean;
}
