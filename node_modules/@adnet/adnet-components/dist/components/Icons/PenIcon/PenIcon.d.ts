import React from 'react';
import { IIconProps } from '../Icon.type';
interface PenIcon extends IIconProps {
    manual?: {
        width: string;
        height: string;
        preserveAspectRatio?: boolean;
    };
    style?: React.CSSProperties;
}
export declare function PenIcon({ color, size, rotate, ...props }: PenIcon): import("react/jsx-runtime").JSX.Element;
export {};
