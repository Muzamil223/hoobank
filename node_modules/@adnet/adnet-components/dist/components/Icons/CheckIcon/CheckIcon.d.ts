import React from 'react';
import { IIconProps } from '../Icon.type';
interface CheckIcon extends IIconProps {
    manual?: {
        width: string;
        height: string;
        preserveAspectRatio?: boolean;
    };
    style?: React.CSSProperties;
}
export declare function CheckIcon({ onClick, color, size, rotate, ...props }: CheckIcon): import("react/jsx-runtime").JSX.Element;
export {};
