import React from 'react';
import { IIconProps } from '../Icon.type';
interface SaveIcon extends IIconProps {
    manual?: {
        width: string;
        height: string;
        preserveAspectRatio?: boolean;
    };
    style?: React.CSSProperties;
}
export declare function SaveIcon({ color, size, rotate, ...props }: SaveIcon): import("react/jsx-runtime").JSX.Element;
export {};
