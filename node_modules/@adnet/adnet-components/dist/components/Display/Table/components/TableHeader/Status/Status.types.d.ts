import { Icons } from '../../../../../../../../../../src/lib/factory/iconFactory';
export declare enum Operator {
    SUM = "sum",
    COUNT = "count"
}
export declare enum Status {
    INPROGRESS = "inProcess",
    FINISHED = "finished",
    STANBY = "standBy",
    INVALID = "invalid"
}
export type TOperation = {
    operator: Operator;
    where: string | null;
};
export interface IStatusData {
    statusId: string;
    label: string;
    icon: Icons;
    measuringUnit: string | null;
    operation: TOperation;
    columnId: number;
    decimalPlaces?: number;
    override?: number;
}
export interface IStatusProps {
    /** An object representing the status item*/
    status: IStatusData;
    /** The numerical value associated with the status*/
    value: number | string;
}
