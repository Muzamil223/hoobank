import { ReactNode } from 'react';
interface ITableFooterProps {
    tableColor?: string;
    customFooter?: ReactNode;
}
declare const TableFooter: ({ tableColor, customFooter }: ITableFooterProps) => import("react/jsx-runtime").JSX.Element;
export default TableFooter;
