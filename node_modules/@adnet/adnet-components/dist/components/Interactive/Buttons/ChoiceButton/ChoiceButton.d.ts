import React from 'react';
interface IChoiceButtonProps {
    /** An array of strings representing the choices that the user can select from. */
    values: Array<string>;
    /** A string that serves as the name attribute for the radio button group. It is used to group related radio buttons together. The default value is 'choiceButton'. */
    buttonName?: string;
    /** A callback function of type React.Dispatch<React.SetStateAction<string>> to update the selected value. This function will be called whenever the user selects a new choice. */
    setSelectedValue: React.Dispatch<React.SetStateAction<string>>;
    /** An optional value that will make the choice button group initialize with that value already checked. */
    defaultValue?: string;
    /** Optional font size override */
    fontSize?: number;
}
export declare const generateUniqueStringId: () => string;
export declare const ChoiceButton: ({ values, buttonName, setSelectedValue, defaultValue, fontSize, }: IChoiceButtonProps) => import("react/jsx-runtime").JSX.Element;
export {};
