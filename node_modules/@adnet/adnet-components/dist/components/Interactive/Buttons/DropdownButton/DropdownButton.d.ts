import { ReactNode } from 'react';
import { ComponentState } from '../../../../constants/ComponentState';
interface IDropdownButtonProps {
    /** A string representing the label or text to display on the button. */
    label: string;
    /** A ReactNode representing the content that should be displayed when the dropdown is expanded. */
    children: ReactNode;
    /** An optional `ComponentState` enum value that defines the state of the component. This is used to visually indicate different states of the component, such as loading or being blocked. */
    state: ComponentState;
    /** is open boolean */
    isOpen: boolean;
    /** Expandable optional parameter */
    expandable?: boolean;
    /** on click handler */
    onClick?: () => void;
}
declare const DropdownButton: ({ label, children, state, isOpen, expandable, onClick }: IDropdownButtonProps) => import("react/jsx-runtime").JSX.Element;
export default DropdownButton;
