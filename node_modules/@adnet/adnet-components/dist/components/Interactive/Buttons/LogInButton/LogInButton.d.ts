import React from 'react';
interface IButtonProps {
    /** A string representing the text to be displayed on the button. */
    text: string;
    /** A callback function of type React.MouseEventHandler<Element> to be executed when the button is clicked. */
    onClick: React.MouseEventHandler<Element>;
    /** A string or null or undefined, representing the text to be displayed as a tooltip when the user hovers over the button. If not provided or set to null  */
    tooltipText: string | null | undefined;
    /** A boolean value indicating whether the button is enabled or disabled. When set to true, the button is enabled and clickable. When set to false, the button becomes disabled and non-clickable. The default value is false. */
    isEnabled?: boolean;
    hasError?: boolean;
}
export default function LogInButton({ text, onClick, tooltipText, isEnabled, hasError }: IButtonProps): import("react/jsx-runtime").JSX.Element;
export {};
