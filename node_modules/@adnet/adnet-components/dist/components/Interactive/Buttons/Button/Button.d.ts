/// <reference types="react" />
import { Size } from '../../../../constants/Size';
import { ComponentState } from '../../../../constants/ComponentState';
interface ButtonProps {
    /** Label for displaying text in the button */
    label: string;
    /** Boolean value to hide the label if needed */
    hideLabel?: boolean;
    /** Component state */
    state?: ComponentState;
    /** onClick handler */
    onClick?: () => void;
    /** Boolean value to hide the icon if needed */
    hideIcon?: boolean;
    /** Boolean value to pad the text */
    padded?: boolean;
    /** Optional size parameter */
    size?: Size;
    /** Background color of the button */
    iconSize?: Size;
    /** Background color of the button */
    backgroundColor?: string;
    /** Text color of the button */
    textColor?: string;
    /** Icon color of the button */
    iconColor?: string;
    /** If the occasion arises, way to override the default styles */
    style?: React.CSSProperties;
    /** Way to define some other icon */
    icon?: React.ReactNode;
}
declare const Button: ({ label, hideLabel, state, onClick, hideIcon, padded, size, backgroundColor, textColor, iconColor, iconSize, style, icon }: ButtonProps) => import("react/jsx-runtime").JSX.Element;
export default Button;
