/// <reference types="react" />
import { ComponentState } from '../../../../constants/ComponentState';
export declare enum NumberInputType {
    NUMBER = "number",
    TEXT = "text"
}
export declare enum MeasuringType {
    EURO = "\u20AC",
    USDOLLAR = "$",
    MEGAWATT = "MW"
}
interface NumberInput {
    /** Provide the type of the input*/
    type?: NumberInputType;
    /** Determines the measuring unit displayed and used in the input*/
    measuringUnit?: string;
    /** The state prop determines the visual state of the `NumberInput` component.*/
    state?: ComponentState;
    /** The given value by the parent component.*/
    value: number;
    /** Setter for the value */
    setValue: React.Dispatch<React.SetStateAction<number>>;
    /** If the occasion arises, way to override the default styles */
    wrapperStyle?: React.CSSProperties;
    /** If the occasion arises, way to override the default styles */
    inputStyle?: React.CSSProperties;
    /** If the occasion arises, way to override the default styles */
    measuringUnitStyle?: React.CSSProperties;
    measuringUnitTextStyle?: React.CSSProperties;
    fontSize?: number;
}
export declare function NumberInput({ type, measuringUnit, state, ...props }: NumberInput): import("react/jsx-runtime").JSX.Element;
export {};
