import React from 'react';
import { ComponentState } from '../../../../constants/ComponentState';
interface IBooleanInputProps {
    /** Boolean value determining the state of the checkbox*/
    isToggled: boolean;
    /** SetState dispatch function for the parent component*/
    setIsToggle: React.Dispatch<React.SetStateAction<boolean>>;
    /** The state of the button */
    currentState?: ComponentState;
    /** Color of the boolean input */
    color?: string;
    /** Color of the outer border */
    borderColor?: string;
}
declare const BooleanInput: ({ isToggled, setIsToggle, currentState, color, borderColor }: IBooleanInputProps) => import("react/jsx-runtime").JSX.Element;
export default BooleanInput;
