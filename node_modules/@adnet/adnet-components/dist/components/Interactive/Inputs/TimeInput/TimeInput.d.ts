import React from 'react';
import { ComponentState } from '../../../../../../../../src/constants/ComponentState.ts';
interface TimeInputProps {
    /** A Date object representing the initial time value. Defaults to the current date and time. */
    value?: number;
    /** A callback function that will be triggered whenever the time value changes. It receives the updated Date object as an argument. */
    setValue?: (value: Date) => void;
    /** A string representing the time zone to be displayed. Defaults to the user's local time zone. */
    timezone?: string;
    /** An optional ComponentState enum value representing the state of the component. This is used to visually indicate different states of the component, such as loading, error, or being blocked. */
    state?: ComponentState;
    /** Optional hide time */
    hideTime?: boolean;
    fontSize?: number;
}
declare const TimeInput: React.FC<TimeInputProps>;
export default TimeInput;
