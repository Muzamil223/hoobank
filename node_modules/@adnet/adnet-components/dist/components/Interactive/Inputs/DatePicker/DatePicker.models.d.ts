export declare enum DayWeekFlags {
    Monday = 0,
    Tuesday = 1,
    Wednesday = 2,
    Thursday = 3,
    Friday = 4,
    Saturday = 5,
    Sunday = 6
}
export interface DayWeekName {
    name: string;
    index: number;
    flag: DayWeekFlags;
}
export interface SelectableDate {
    date: Date;
    groupOwnerId?: number;
    color?: string;
}
export interface DateInMonth {
    date: Date;
    isAvailable: boolean;
    isAllowed: boolean;
    blank?: boolean;
    timestamp?: string;
    dayOfweek?: number;
}
export interface MonthWrapedDays {
    monthName: string;
    availableDates: DateInMonth[];
    year: number;
}
