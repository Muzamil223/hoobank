import { DayWeekName } from './DatePicker.models';
export declare const generateDateNamesInWeek: (langCode: string) => DayWeekName[];
export declare const dateToYYYYMMDDstring: (date: Date) => string;
export declare const generateFullYearBlocks: (props: {
    langCode: string;
    year: number;
}) => {
    monthName: string;
    availableDates: {
        date: Date;
        isAvailable: boolean;
        isAllowed: boolean;
        blank: boolean;
        timestamp: string;
        dayOfweek: number;
    }[];
    year: number;
}[];
