import React, { Dispatch, SetStateAction } from 'react';
import { IIconProps } from '../../../Icons/Icon.type';
interface checkboxProps extends IIconProps {
    /** Label for displaying text in the checkbox */
    label?: string;
    /**Option to check checkbox */
    checked: boolean;
    setChecked: Dispatch<SetStateAction<boolean>>;
    /**Option to disable checkbox*/
    disabled?: boolean;
    /**Font size */
    fontSize?: string;
    /**Checkbox size*/
    cubeSize?: string;
    /** Background color of the checkbox */
    backgroundColor?: string;
    /** Border color of the checkbox */
    borderColor?: string;
    /**Option to activate border */
    borderWidth?: string;
    /** Option to set border type */
    borderStyle?: string;
    /** Text color of the checkbox */
    textColor?: string;
    /** Color of the icon */
    iconColorChecked?: string;
    iconColorUnChecked?: string;
    /** If the occasion arises, a way to override the default styles */
    style?: React.CSSProperties;
}
declare const CheckBoxInput: ({ label, checked, disabled, fontSize, cubeSize, backgroundColor, borderColor, borderWidth, borderStyle, textColor, iconColorChecked, iconColorUnChecked, style, setChecked, }: checkboxProps) => import("react/jsx-runtime").JSX.Element;
export default CheckBoxInput;
