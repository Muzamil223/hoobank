import React, { Dispatch, ReactNode } from 'react';
import { IGlobalActionType } from './global.interface';
import { IGlobalState } from './global.models';
export declare const initialState: IGlobalState;
declare const GlobalContext: React.Context<{
    state: IGlobalState;
    dispatch: Dispatch<IGlobalActionType>;
}>;
declare const GlobalProvider: React.FC<{
    children: ReactNode;
}>;
export declare const useGlobalContext: () => {
    state: IGlobalState;
    dispatch: Dispatch<IGlobalActionType>;
};
export { GlobalContext, GlobalProvider };
